# Follower sprites pack

Copy this folder and rename it to your pack ID. Replace
`followers/pikachu.png` with a four-column, four-direction PNG sheet. Keep its
filename identical to the `file` value in `mod.json`. Use `pikachu:shiny` for a
shiny version.
